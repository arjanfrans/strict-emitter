'use strict';

const chai = require('chai');
const expect = chai.expect;

const createEventDefinitions = require('../lib/').createEventDefinitions;
const createEvent = require('../lib/').createEvent;
